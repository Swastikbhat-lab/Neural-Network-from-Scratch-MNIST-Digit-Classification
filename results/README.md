# Results Directory

Training outputs will be saved here.

Expected files after training:
- `model_final.pkl` - Trained model weights
- `training_history.json` - Loss and accuracy per epoch
- `training_curves.png` - Visualization of training progress
- `predictions.npy` - Model predictions on test set

These files are generated automatically by `train.py`.
